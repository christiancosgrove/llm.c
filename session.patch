diff --git a/doc/layernorm/layernorm.c b/doc/layernorm/layernorm.c
index 67aa40c..15c675f 100644
--- a/doc/layernorm/layernorm.c
+++ b/doc/layernorm/layernorm.c
@@ -9,34 +9,57 @@
 void layernorm_forward(float* out, float* mean, float* rstd,
                        float* inp, float* weight, float* bias,
                        int B, int T, int C) {
-    float eps = 1e-5f;
+    const float eps = 1e-5f;
+    const float inv_C = 1.0f / C;
+    
     for (int b = 0; b < B; b++) {
         for (int t = 0; t < T; t++) {
             // seek to the input position inp[b,t,:]
             float* x = inp + b * T * C + t * C;
-            // calculate the mean
-            float m = 0.0f;
-            for (int i = 0; i < C; i++) {
-                m += x[i];
+            float* out_bt = out + b * T * C + t * C;
+            
+            // calculate mean and variance in single pass
+            float sum = 0.0f;
+            float sum_sq = 0.0f;
+            
+            // Unroll loop by 4 for better performance
+            int i = 0;
+            for (; i < C - 3; i += 4) {
+                float x0 = x[i], x1 = x[i+1], x2 = x[i+2], x3 = x[i+3];
+                sum += x0 + x1 + x2 + x3;
+                sum_sq += x0*x0 + x1*x1 + x2*x2 + x3*x3;
             }
-            m = m/C;
-            // calculate the variance (without any bias correction)
-            float v = 0.0f;
-            for (int i = 0; i < C; i++) {
-                float xshift = x[i] - m;
-                v += xshift * xshift;
+            // Handle remaining elements
+            for (; i < C; i++) {
+                float xi = x[i];
+                sum += xi;
+                sum_sq += xi * xi;
             }
-            v = v/C;
-            // calculate the rstd
+            
+            float m = sum * inv_C;
+            float v = sum_sq * inv_C - m * m;
             float s = 1.0f / sqrtf(v + eps);
-            // seek to the output position in out[b,t,:]
-            float* out_bt = out + b * T * C + t * C;
-            for (int i = 0; i < C; i++) {
-                float n = (s * (x[i] - m)); // normalized output
-                float o = n * weight[i] + bias[i]; // scale and shift it
-                out_bt[i] = o; // write
+            
+            // Apply normalization, scale and bias in single pass
+            i = 0;
+            for (; i < C - 3; i += 4) {
+                float n0 = s * (x[i] - m);
+                float n1 = s * (x[i+1] - m);
+                float n2 = s * (x[i+2] - m);
+                float n3 = s * (x[i+3] - m);
+                
+                out_bt[i]   = n0 * weight[i]   + bias[i];
+                out_bt[i+1] = n1 * weight[i+1] + bias[i+1];
+                out_bt[i+2] = n2 * weight[i+2] + bias[i+2];
+                out_bt[i+3] = n3 * weight[i+3] + bias[i+3];
+            }
+            // Handle remaining elements
+            for (; i < C; i++) {
+                float n = s * (x[i] - m);
+                out_bt[i] = n * weight[i] + bias[i];
             }
-            // cache the mean and rstd for the backward pass later
+            
+            // cache the mean and rstd
             mean[b * T + t] = m;
             rstd[b * T + t] = s;
         }
diff --git a/optimization_results.json b/optimization_results.json
index 5f2b3d4..11497c1 100644
--- a/optimization_results.json
+++ b/optimization_results.json
@@ -1,3 +1,3 @@
 {
-  "score": 0.275891
+  "score": 0.275159
 }
\ No newline at end of file
